# Python 3.10+ is recommended for SAM2

# PyTorch (Version >= 2.3.1 with CUDA 12.1, as recommended for SAM2)
# Find the exact install command from: https://pytorch.org/get-started/locally/
# Ensure your Kubeflow environment supports CUDA 12.1 if using GPU.
torch==2.3.1+cu121
torchvision==0.18.1+cu121 # Matched to PyTorch 2.3.1
torchaudio==2.3.1+cu121 # Matched to PyTorch 2.3.1
# If you need a CPU-only version, comment the lines above and uncomment these:
# torch==2.3.1
# torchvision==0.18.1
# torchaudio==2.3.1

# SAM2 from the official Facebook Research GitHub repository
# This installs the package 'sam2'
git+https://github.com/facebookresearch/sam2.git
# Alternative name for the same repository (segment-anything-fast-2)
# git+https://github.com/facebookresearch/segment-anything-fast-2.git

# Geospatial raster data handling
rasterio

# Standard data science and image libraries
numpy
matplotlib
Pillow

# Jupyter environment essentials
ipykernel   # For registering this environment as a Jupyter kernel
jupyterlab  # To run a JupyterLab server
# notebook  # Uncomment if you prefer the classic Jupyter Notebook interface
